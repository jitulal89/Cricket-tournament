V19 is based directly on V13. It preserves the V13 page flow and UI.
Fixes only: captain claim RPC uses p_tournament (actual function signature), captain bid RPC uses p_team_id (actual function signature), and getTournament gives a clear message when slug is missing. No testing bypass.
